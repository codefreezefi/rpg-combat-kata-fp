/**
 * Default and maximum health of a character
 */
const DEFAULT_AND_MAX_CHARACTER_HEALTH = 1000

const DEFAULT_PROP_HEALTH = 1000

/**
 * Starting level of a character
 */
const START_LEVEL = 1

const MELEE_FIGHTER = 'melee'
const RANGED_FIGHTER = 'ranged'

const classes = [
  MELEE_FIGHTER,
  RANGED_FIGHTER
]

module.exports = {
  DEFAULT_AND_MAX_CHARACTER_HEALTH,
  DEFAULT_PROP_HEALTH,
  START_LEVEL,
  classes,
  MELEE_FIGHTER,
  RANGED_FIGHTER
}
